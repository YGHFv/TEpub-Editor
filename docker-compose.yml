services:
  tepub-web:
    image: yghf/tepub-editor-web:latest
    container_name: tepub-editor-web
    ports:
      - "${TEPUB_WEB_PORT:-5233}:5233"
    volumes:
      - tepub-web-data:/data
    restart: unless-stopped

volumes:
  tepub-web-data:
